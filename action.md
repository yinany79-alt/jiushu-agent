# 九数算法中台：全链路大模型微调 Agentic Demo 设计 (V5.0 - 终极版)

## 1. 工业级微调全链路步骤定义 (The Full Workflow)

为了演示的专业性，我们将 Llama-3 的微调任务拆解为以下 6 个核心阶段：

1. **环境与项目初始化 (Init):** 确定 Workspace、基础模型版本（如 Llama-3-8B-Instruct）。
2. **数据准备与分析 (Data Prep):** 选择训练/验证集，展示数据分布采样，确认 Prompt Template。
3. **资源算力配比 (Resource):** 选择 GPU 规格（A100/H800）、节点数量、分布式并行策略。
4. **超参数精细化配置 (Hyperparams):** 配置学习率、Epochs、LoRA Rank/Alpha、微调配置（Full/LoRA/QLoRA）。
5. **任务启动与状态观测 (Execution):** 提交 K8s 任务，实时监控 Loss 曲线、吞吐量、显存占用。
6. **评估与模型导出 (Eval & Export):** 评估 PPL (困惑度)，指标对比，模型一键转换并存入模型中心。

---

## 2. 核心演示脚本与交互协议 (Scenario Logic)

请 Claude 基于以下逻辑实现 **“阻塞式联动”**：

### 节点 1：项目初始化 (Project Setup)
* **Agent 思考:** "正在识别用户权限，检索可用算力分区... 建议在 `Space_Yveson_Pro` 进行实验。"
* **交互卡片:** `[单选卡片]`：请确认空间与基础模型 (Llama-3-8B / Llama-3-70B)。
* **画布联动:** 用户确认后，Node1 [项目初始化] 变绿。

### 节点 2：数据准备 (Data Strategy)
* **Agent 思考:** "正在扫描 BDP 存储... 发现 `dataset_llama_v1`。正在进行 Token 分布采样分析..."
* **交互卡片:** `[多选卡片]`：选择训练集 (Train) 与 验证集 (Eval)；`[下拉卡片]`：确认 Prompt 模板。
* **画布联动:** 用户确认后，Node2 [数据预处理] 开启流光效果。

### 节点 3：算力资源配比 (GPU Cluster)
* **Agent 思考:** "计算模型参数量中... 建议使用 4 * A100 (80G) 以开启 DeepSpeed ZeRO-3 优化。"
* **交互卡片:** `[推荐位卡片]`：A100 * 4 (推荐) / H800 * 2 / A100 * 8。
* **画布联动:** 用户确认后，Node3 [算力分配] 变绿。

### 节点 4：超参确认 (Hyperparameter Tuning)
* **Agent 思考:** "根据数据集大小，已为你预设最佳学习率 2e-5。LoRA Rank 建议设置为 8。"
* **交互卡片:** `[滑动条卡片]`：调整 Learning Rate, Batch Size, LoRA Rank。
* **画布联动:** 用户确认后，Node4 [任务配置] 变绿。

### 节点 5：监控与指标 (Live Monitoring)
* **Agent 动作:** 自动提交 K8s 任务，抽屉进入“执行态”。
* **动态组件:** 抽屉内展示 **实时 Loss 曲线图 (Recharts)**，每 2 秒模拟一个数据点下探。
* **画布联动:** Node5 [模型训练] 进入蓝色 Loading 呼吸灯状态。

### 节点 6：归档导出 (Artifacts)
* **Agent 思考:** "训练完成，验证集 PPL 达到 1.08，优于 Baseline。正在打包模型权重..."
* **交互卡片:** `[按钮组]`：一键部署到在线推理 / 导出到模型中心。
* **画布联动:** Node6 [模型导出] 变绿。

---

## 3. 给 Claude Code 的技术实现指令

> **"Claude, 请严格按照以下要求修改 React 代码："**
>
> 1. **状态驱动:** 所有的步骤由一个 `activeStep` 索引控制。每一步必须在 `Drawer` 中显示对应的 `AgentMessage` 和 `ActionCard`。
> 2. **画布渲染 (ReactFlow):** >    - 初始状态：画布上显示 6 个灰色虚线节点。
>    - 激活状态：当前步骤节点显示为蓝色脉冲效果。
>    - 完成状态：点击卡片提交后，节点变为绿色，连线变为流光。
> 3. **去粘贴化设计:** >    - 所有的卡片选项必须是写死的 Mock 数据（针对演示场景优化）。
>    - 用户不允许输入任何字符，只能通过点击完成全流程。
> 4. **指标模拟:** >    - 在第五步时，请务必集成一个折线图组件，展示 Loss 随 Step 下降的趋势。
> 5. **交互流畅度:** >    - 在用户点击“确认”后，设置 1000ms 的 `setThinking(true)` 状态，模拟 Agent 在后台处理逻辑。