# 九数算法中台：监控中心 - 自动托管 (Autonomous Management) 深度规格书 (V15.0)

## 1. 视觉与交互规范 (Design System & Motion)
* **视觉调性：** 放弃大面积紫色渐变。采用 **极简白/灰基底 + 极窄边框亮色提醒,尽可能符合整体平台设计风格**。
* **卡片设计：** 采用层级化的阴影（Shadow）和微弱的毛玻璃（Glassmorphism）效果。
* **动效规范：** 所有的状态切换（如从“待机”到“自愈中”）必须遵循 `cubic-bezier(0.4, 0, 0.2, 1)` 的平滑过渡，时长控制在 300ms。

---

## 2. 页面结构与功能定义

### 2.1 顶部：全站托管概览 (Autonomous Intelligence Summary)
* **UI 特征：** 纯白卡片，左侧带一条 4px 宽的品牌色（如深蓝或淡紫）垂直线条作为引导。
* **内容逻辑：** - **左侧：** 实时滚动的“托管日志流”（简短的一行式文字，如：12:05 自动扩容 Service_A）。
    - **右侧：** AI 智能摘要。用极简的排版列出核心 KPI：[已节约算力]、[自动修复任务数]、[当前拦截风险]。
* **交互：** 点击摘要可展开查看“Agent 深度治理报告”。

### 2.2 中部：动态策略执行流 (Live Execution Flow)
这是页面的“心脏”，展示 Agent 正在处理的实时动作。
* **卡片状态机规范 (State Machine)：**
    1.  **监控中 (Monitoring)：** 静态展示，标签为“策略已激活”，背景纯净。
    2.  **决策中 (Decision-Making)：** 卡片边框出现微弱的脉冲光晕，文字提示“Agent 正在分析异常日志...”。
    3.  **执行中 (Action-InProgress)：** 出现线性进度条。例如：[ 正在重启容器: 65% ]。
    4.  **结果回馈 (Feedback)：** 成功则短暂高亮绿色并收回；失败则变红并常驻，提示“需要人工介入”。

### 2.3 底部：策略配置矩阵 (Policy Configuration Matrix)
**严格标准化的策略卡片，包含四个核心能力：**
1.  **自愈 (Self-Healing)：** 训练失败自动重启。配置项：错误码白名单、重启上限。
2.  **弹性 (Elasticity)：** 推理服务自动扩缩容。配置项：QPS 阈值、CPU/显存水位。
3.  **接管 (Takeover)：** 常见告警（磁盘/网络）自动清理。配置项：清理规则、紧急停机策略。
4.  **调度 (Scheduling)：** 研发脚本周期性托管。配置项：Cron 表达式、产出校验逻辑。

---

## 3. 核心交互逻辑与 Mock 数据

### 3.1 策略管理抽屉 (Policy Editor Drawer)
当用户点击底部策略的“配置”时，侧边滑出抽屉：
* **表单规范：** 采用标准的 Select 和 Slider 组件，禁止自由文本输入。
* **模拟预览：** 在抽屉底部显示“策略模拟预估”，Agent 告诉用户：“基于此配置，预计每月可减少 15% 的空置算力浪费”。

### 3.2 AI 助手联动 (Agent Assistant)
* 页面右下角悬浮一个极简的 Agent 图标。
* **交互：** 用户点击图标，Agent 会基于“自动托管”页面的数据直接对话，例如：“Yveson，最近 Llama3 任务频繁 OOM，我已自动将重试等待时间延长，你需要我帮你直接修改研发模式下的初始显存配置吗？”

---

## 4. Claude Code 执行指令 (Action Tasks)

### 第一阶段：页面重构与布局
- [ ] 在监控中心 Tab 栏增加 [自动托管] 选项。
- [ ] 按照“概览卡片 -> 动态执行流 -> 策略列表”的顺序完成布局，要求视觉极其纯净、专业。

### 第二阶段：状态切换动效
- [ ] 为执行流卡片编写状态切换逻辑。实现从“监控”到“执行”的平滑视觉过渡（边框光晕、进度条显隐）。
- [ ] 实现策略配置开关（Switch）的防抖处理和即时反馈。

### 第三阶段：Mock 场景闭环
- [ ] 预置一个动态场景：点击页面上的“模拟异常”，观察 Agent 如何自动生成自愈卡片、展示进度条、最后在顶部摘要更新修复记录。
- [ ] 确保所有的组件（列表、详情、抽屉）在 Tailwind 下拥有高度一致的圆角（rounded-xl）和字体比例。

---

## 5. 演示话术
> “我们不需要炫酷的颜色来标榜 AI，我们要的是结果的确定性。在『自动托管』页面，每一行进度条都代表 Agent 为算法工程师节省了一次半夜起床处理报错的时间。”