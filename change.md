# 九数算法中台：侧边栏重构与独立“智能开发”模块内嵌方案 (V13.0)

## 1. 侧边栏导航架构重排 (Sidebar Layout)
目标：提升 Agentic 模块权重，保持系统功能底座完整。

### 1.1 顶层：智能入口 (Agentic Tier)
* **智能中枢 (AI Hub)**
    * **状态：** 保持现有的 [问答] + [任务编排/执行] 逻辑，原来智能开发改名字为智能中枢，**严禁修改其内部代码**。
* **智能开发 (Intelligent Dev)**
    * **状态：** 新增模块。
    * **实现：** 采用独立页面内嵌方式（详见第 2 节）。
* **监控中心 (Monitoring Center)**
    * **状态：** 新增模块（包含总览与追踪 Tab）。

### 1.2 底层：系统功能 (Legacy Tier)
在上方增加分割线“系统功能”，并严格复刻图 2 中的所有项：
* **工作空间：** 工作空间详情。
* **模型服务：** 模型广场、体验中心、在线推理、离线推理。
* **模型训练：** 模型开发、模型精调、模型管理。
* **资产管理：** 数据管理 (二级菜单)、镜像管理。
* **AIFlow工作流：** 工作流开发 (红 New 标签)、组件管理。

---

## 2. 智能开发 (Intelligent Dev) 实现：HTML 内嵌模式

为了保证现有系统稳定性，对 [智能开发] 模块采用**非侵入式**集成：

* **路由定义：** 新增 `/dev-mode` 路由。
* **实现方式：** - 创建一个全屏容器组件 `DevContainer`。
    - 使用 `<iframe>` 或直接将用户提供的 HTML 字符串注入 `dangerouslySetInnerHTML`。
    - **隔离性：** 该模块不与“智能中枢”进行任何状态通信（State Sharing），确保独立运行。
* **视觉适配：** 确保内嵌页面能够自适应侧边栏宽度，保持布局不溢出。

---

## 3. 监控中心 (Monitoring Center) 简述
* **功能：** 实现“监控总览（看板+任务列表）”和“模型追踪（血缘图）”两个 Tab 页。
* **数据：** 全部采用 Mock 数据，确保演示流畅。

---

## 4. Claude Code 执行指令 (Implementation Instructions)

### 4.1 侧边栏与路由 (Critical)
- [ ] 按照“智能中枢 -> 智能开发 -> 监控中心”顺序重排顶部导航。
- [ ] 完整复刻图 2 底部所有分类功能，确保图标、文字、New 标签完全一致。
- [ ] 确保点击侧边栏时，能正确切换路由并高亮对应项。

### 4.2 模块嵌入 (Intelligent Dev)
- [ ] **不要**尝试重构用户提供的 HTML/JS 代码。
- [ ] 只需将其作为一个独立 Page 挂载到 `/dev-mode`，通过简单容器展示。

### 4.3 保护现有逻辑 (Safety)
- [ ] **禁止修改** `AI Hub (智能中枢)` 内部的问答、执行逻辑及意图转换代码。

### 4.4 UI 细节
- [ ] 确保侧边栏在不同分辨率下的滚动体验（顶部固定，底部系统功能区自适应滚动）。